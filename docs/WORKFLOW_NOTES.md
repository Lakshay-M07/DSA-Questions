# Workflow Notes

The submission sync workflow is intentionally scheduled and can also be started manually.

Changes to automation should preserve its platform checks, regression tests, and safe dashboard generation step.
